import {NextResponse} from 'next/server';import {z} from 'zod';import {prisma} from '@/lib/prisma';import {currentUser} from '@/lib/current-user';
const schema=z.object({name:z.string().min(1),sku:z.string().min(1),category:z.string().optional().nullable(),minStock:z.number().int().min(0),unit:z.string().min(1)});
async function auth(){return currentUser()}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const user=await auth();if(!user)return NextResponse.json({ok:false,message:'Não autenticado'},{status:401});if(user.role==='VIEWER')return NextResponse.json({ok:false,message:'Seu perfil permite apenas consultas.'},{status:403});try{const b=schema.parse(await req.json());const p=await prisma.product.update({where:{id:(await params).id},data:b});return NextResponse.json({ok:true,data:p});}catch(e:any){return NextResponse.json({ok:false,message:e?.code==='P2002'?'SKU já cadastrado.':'Produto não encontrado.'},{status:400});}}
export async function DELETE(req:Request,{params}:{params:Promise<{id:string}>}){const user=await auth();if(!user)return NextResponse.json({ok:false,message:'Não autenticado'},{status:401});if(user.role!=='ADMIN')return NextResponse.json({ok:false,message:'Apenas o administrador pode excluir.'},{status:403});await prisma.product.update({where:{id:(await params).id},data:{active:false}});return NextResponse.json({ok:true});}
