import {NextResponse} from 'next/server';import {z} from 'zod';import {prisma} from '@/lib/prisma';import {cookies} from 'next/headers';import {verifySession,COOKIE} from '@/lib/auth';
const schema=z.object({name:z.string().min(1),sku:z.string().min(1),category:z.string().optional().nullable(),minStock:z.number().int().min(0),unit:z.string().min(1)});
async function auth(){return verifySession((await cookies()).get(COOKIE)?.value)}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({ok:false,message:'Não autenticado'},{status:401});try{const b=schema.parse(await req.json());const p=await prisma.product.update({where:{id:(await params).id},data:b});return NextResponse.json({ok:true,data:p});}catch(e:any){return NextResponse.json({ok:false,message:e?.code==='P2002'?'SKU já cadastrado.':'Produto não encontrado.'},{status:400});}}
export async function DELETE(req:Request,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({ok:false,message:'Não autenticado'},{status:401});await prisma.product.update({where:{id:(await params).id},data:{active:false}});return NextResponse.json({ok:true});}
